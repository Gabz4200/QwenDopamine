defaults:
  - /train: frozen_backbone
  - _self_

experiment:
  name: ablation_unfreezing

freeze:
  base_model: true
  new_blocks: false
  unfreeze_phases:
    - step: 5000
      modules: [model.layers.2]
    - step: 10000
      modules: [model.layers.4]
    - step: 15000
      modules: [model]
